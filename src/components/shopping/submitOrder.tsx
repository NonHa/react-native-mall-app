import React, { useEffect, useState, useRef } from 'react';
import {
  Text,
  View,
  StyleSheet,
  ScrollView,
  Image,
  SafeAreaView,
  Modal,
  TouchableHighlight,
} from 'react-native';

export default function SubmitOrder(props) {
  return (
    <SafeAreaView>
      <ScrollView>
        <View>
          <Text>32432</Text>
        </View>
      </ScrollView>
    </SafeAreaView>
  );
}
